/** Payload req.context flag: skip auto-create locale siblings on nested page creates. */
export const SKIP_LOCALE_AUTO_CREATE = 'skipLocaleAutoCreate'

/** Payload req.context flag: skip pushing structure to siblings (prevents update loops). */
export const SKIP_LOCALE_STRUCTURE_SYNC = 'skipLocaleStructureSync'
